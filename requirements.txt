# PromptFuse dependencies (Step 0.3)
#
# Quick install:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# GPU workstation (RTX 6000 Ada / Linux or WSL2):
#   1. Install PyTorch with CUDA first (see https://pytorch.org):
#        pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   2. Then install the rest:
#        pip install -r requirements.txt
#
# Notes:
#   - vLLM requires Linux or WSL2 with an NVIDIA GPU.
#   - On macOS, omit the vLLM line or use requirements-core.txt if you split later.
#   - Hugging Face token required for Llama models: export HF_TOKEN=...

# --- Core ---
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0
numpy>=1.26
faiss-cpu>=1.8
sentence-transformers>=3.0
transformers>=4.40
torch>=2.2
nltk>=3.8
rouge-score>=0.1.2
datasets>=2.18
tqdm>=4.66
scikit-learn>=1.4

# --- Development & testing ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.3

# --- Serving (vLLM + prefix caching; Linux/WSL2 + CUDA) ---
vllm>=0.6

# --- Baselines (LLMLingua comparison) ---
llmlingua>=0.2

# --- Install PromptFuse in editable mode ---
-e .
